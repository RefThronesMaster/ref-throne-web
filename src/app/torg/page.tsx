"use client";

import Image from "next/image";

import React from "react";

export default function Home() {
  // const { accounts, reqAccounts, getPermissions } =
  //   React.useContext(AppContext);

  return (
    <div className="mt-10">
      <div className="flex">gtor</div>
    </div>
  );
}
